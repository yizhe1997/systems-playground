#!/bin/bash
# Test entrypoint for <project-name>. CI and contributors both call this script,
# so it should be the single source of truth for "does this project pass."

set -euo pipefail
echo "TODO: implement test.sh for <project-name>"
