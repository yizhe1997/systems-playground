#!/bin/bash
# Local dev entrypoint for <project-name>.
# Fill in with whatever "start everything for local development" means for this project
# (e.g. `docker compose -f ../docker-compose.project.yml up --build`).

set -euo pipefail
echo "TODO: implement dev.sh for <project-name>"
