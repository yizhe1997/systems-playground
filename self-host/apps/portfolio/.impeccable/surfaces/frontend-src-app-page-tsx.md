---
version: 1
slug: "frontend-src-app-page-tsx"
primary_target: "frontend/src/app/page.tsx"
related_targets: []
---

## Scope and visitor mode
Home page, `frontend/src/app/page.tsx`. Persuade mode — the entry point every visitor hits first.

## Audience, job, action
Recruiter screening in seconds. Job: decide whether to request the resume. Primary action: resume-request submit (existing `/api/resume/request` endpoint, unchanged).

## Proof / content
Neo-Brutalist direction (pinned by operator from an external reference, supersedes two prior rejected directions - "Config File" and "Technical Blueprint"/Mosaic Grid). Yellow hero with dot-pattern texture, bold Cabinet Grotesk headline with one -webkit-text-stroke keyword, browser-mockup panel showing real stack info (not fabricated data). Featured Projects and Docs as bento-grid cards with hard offset shadows and 2px black borders, empty-state where no real content exists yet (Evidence on Hand is TBD). Docs section on a dark charcoal panel for section-to-section contrast.

## Memorable moment
The physical "press" interaction on every button (translate into its own shadow on hover) plus the hard-shadow/thick-border system throughout — bold and confident rather than the flat/quiet feel of the two prior rejected directions.

## Unresolved
- Exact featured-projects/docs count once real content exists (currently 0, empty state).
- Nav order/labels once all 6 surfaces exist (only Home is being built first).
- Testimonials/personas/social-proof marquee from the source SaaS reference were deliberately not built - would require fabricating fake customers, which breaks PRODUCT.md's "no invented evidence" principle.
