# <project-name>

One-paragraph summary of what this project demonstrates.

## Scope

- Primary use case:
- Intended audience:
- Out of scope:

## Local Development

- Copy `.env.example` to `.env` and fill placeholders.
- Start dependencies with `docker-compose.project.yml` if needed.
- Run project dev script from `scripts/dev.sh`.

## Testing

- Run `scripts/test.sh`.
- Add integration/e2e tests under `tests/`.

## Interfaces

Document exposed APIs/events/contracts and where they are defined in `contracts/`.
