# Production Override
# Use this when running via Watchtower or pulling pre-built images.
# Run with: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  backend:
    image: ${DOCKER_USERNAME:-yourdockerhub}/systems-playground-backend:latest
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

  frontend:
    image: ${DOCKER_USERNAME:-yourdockerhub}/systems-playground-frontend:latest
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
