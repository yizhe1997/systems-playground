name: dsh - Send Launch Link
# Manual only, on purpose: ADR 001 forbids pull_request triggers on the self-hosted runner. Run it from the
# GitHub website or mobile app (Actions > "dsh - Send Launch Link" > Run workflow) and the dsh
# launch link arrives in the Discord channel behind DSH_LINK_WEBHOOK_URL. The link is masked in this
# (public) run log and never printed.
on:
  workflow_dispatch:

jobs:
  send-link:
    runs-on: self-hosted
    env:
      INFRA_BASE_DIR: ${{ vars.INFRA_BASE_DIR }}
      INFISICAL_CLIENT_ID: ${{ vars.INFISICAL_CLIENT_ID }}
      INFISICAL_CLIENT_SECRET: ${{ secrets.INFISICAL_CLIENT_SECRET }}
      INFISICAL_PROJECT_ID: ${{ vars.INFISICAL_PROJECT_ID }}
      INFISICAL_DOMAIN: ${{ vars.INFISICAL_SITE_URL || 'http://localhost:8090' }}

    steps:
      - name: Install Infisical CLI
        run: |
          LATEST_TAG=$(curl -sL -o /dev/null -w '%{url_effective}' https://github.com/Infisical/cli/releases/latest | sed 's#.*/tag/##')
          VERSION="${LATEST_TAG#v}"
          curl -sL "https://github.com/Infisical/cli/releases/download/${LATEST_TAG}/cli_${VERSION}_linux_amd64.tar.gz" -o infisical-cli.tar.gz
          mkdir -p "$HOME/.local/bin"
          tar -xzf infisical-cli.tar.gz -C "$HOME/.local/bin" infisical
          chmod +x "$HOME/.local/bin/infisical"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          rm infisical-cli.tar.gz

      - name: Authenticate to Infisical
        id: infisical-auth
        run: |
          : "${INFISICAL_CLIENT_ID:?INFISICAL_CLIENT_ID repository variable must be set}"
          : "${INFISICAL_CLIENT_SECRET:?INFISICAL_CLIENT_SECRET repository secret must be set}"
          : "${INFISICAL_PROJECT_ID:?INFISICAL_PROJECT_ID repository variable must be set}"
          TOKEN=$(infisical login --domain="$INFISICAL_DOMAIN" --method=universal-auth \
            --client-id="$INFISICAL_CLIENT_ID" --client-secret="$INFISICAL_CLIENT_SECRET" \
            --plain --silent)
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> "$GITHUB_OUTPUT"

      - name: Send launch link
        env:
          INFISICAL_TOKEN: ${{ steps.infisical-auth.outputs.token }}
        run: |
          infisical run --domain="$INFISICAL_DOMAIN" --token="$INFISICAL_TOKEN" \
            --projectId="$INFISICAL_PROJECT_ID" --env=prod -- bash -c '
          set -e
          : "${INFRA_BASE_DIR:?INFRA_BASE_DIR repository variable must be set}"
          : "${DSH_LINK_WEBHOOK_URL:?DSH_LINK_WEBHOOK_URL must be set in Infisical (a Discord webhook for a private channel)}"
          echo "::add-mask::$DSH_LINK_WEBHOOK_URL"

          cd "$INFRA_BASE_DIR/dsh" || { echo "❌ $INFRA_BASE_DIR/dsh not found - deploy dsh first."; exit 1; }
          LINK=$(sh link.sh)
          echo "::add-mask::$LINK"
          echo "::add-mask::${LINK#*token=}"

          # The link holds only URL-safe characters, so it can be embedded in the JSON body as is.
          curl -sS --fail -o /dev/null -H "Content-Type: application/json" \
            -d "{\"content\":\"dsh launch link (open it once per browser; anyone with it can sign in until dsh next restarts): $LINK\"}" \
            "$DSH_LINK_WEBHOOK_URL"
          echo "✅ Launch link sent to Discord."
          '
