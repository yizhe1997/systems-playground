name: Deploy to NUC (Self-Hosted)

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - 'README.md'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Trigger Zero-Downtime Deployment
        run: |
          # bash /home/user/apps/systems-playground/infra/scripts/deploy-playground.sh